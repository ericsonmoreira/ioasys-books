import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#AB2680',
    },
    text: {
      primary: '#333333',
    },
  },
  typography: {
    fontFamily: 'Heebo',
  },
});
